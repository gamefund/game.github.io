loadModule();

window.onload = function () {
    //显示交易所
    // uName = "猫主";
    // $.post(Config.address + "guestLogin", {
    //     uName: uName
    // }, function (data) {
    //     console.log("Login Succes");
    //     try {
    //         if (data.error == undefined) {
    //             userId = data.msg;
    //             $(".kitties-sign-in").html(uName);
    //         }
    //     } catch (e) {
    //         alert("Login Failed");
    //     }
    // }, 'json');
    userId = 1;

    marketInit();
};
