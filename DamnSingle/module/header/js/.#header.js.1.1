/**
 * kitties header
 */
var headerInit = function(pageData){
    $("body").append(headerText);
    if(pageData.type!=OperateType.OPEN_EXCHANGE){
        $(".kitties-logo").before(headerReturnText);
    }

    $(".kitties-sign-in").click(function(){
        //启动我的猫界面

    });

    $(".kitties-marketplace").click(function(){
        marketInit();
    });

    $(".header-return").click(function(){
        if(pageData.type==OperateType.OPEN_EXCHANGE){
            
        }else if(pageData.type==OperateType.OPEN_DETAIL){
            marketInit(data);
        }else if(pageData.type==OperateType.KITTIES){
            kittyInit(kitty_detail);
        }else if(pageData.type==OperateType.KITTIES_SIRE){
            kittyInit(kitty_detail);
        }else if(pageData.type==OperateType.KITTIES_BUY){
            kittyInit(kitty_detail);
        }else if(pageData.type==OperateType.KITTIES_SALE){
            kittyInit(kitty_detail);
        }
    });
}
